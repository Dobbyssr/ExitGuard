# backend/.env 로 복사해서 로컬 값 지정. (실제 .env는 gitignore — 커밋 금지)
# docker-compose.yml의 POSTGRES_*와 일치. 호스트 포트 5433(기존 네이티브 PG가 5432 점유).
DATABASE_URL=postgresql+pg8000://exitguard:exitguard@localhost:5433/exitguard
