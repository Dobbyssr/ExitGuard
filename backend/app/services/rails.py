"""레일(rails) 도메인 서비스 자리.

유비쿼터스 언어: '레일' = 노무 / 영업비밀 / 보안 3개 방어 축.
레일별 게이트 상태·미충족 항목 집계 로직이 여기 모인다. 실제 기준 대조는 compare.py가 맡고,
이 파일은 그 결과를 레일 단위로 묶는다.

API 계약(docs/spec/api-spec.md, data-model.md) 확정 후 구현. 지금은 도메인 경계만 선언.
"""
