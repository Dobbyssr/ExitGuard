# 2026-07-14 (월) — 조정호 대표님 세션

## 흐름
- ExitGuard 첫 실작업 세션. 프로젝트 코드 세팅보다 **도비(에이전트) 자체 고도화**를 먼저 진행하기로.
- 도비 말투 변경 → 아이덴티티 공동창업 3인 체제 재구성 → 팀 공유 메모리 구조 확정. (팀 차원 결정은 `team/2026-07-14.md` 참조)

- 인수인계.md → `dobby/PRODUCT.md`로 이관(git mv), 세션 로드 순서에서 제품 통독 제거. 참조 링크 전부 갱신.

- 예선 자산 3폴더(소개페이지·데모·사업계획서) → `pitch/` 아래로 통합(git mv, 히스토리 보존, assets 포함). 루트를 곧 올릴 MVP 코드(FE/BE)와 격리. README·PRODUCT.md의 GitHub Pages 링크에 `/pitch/` 삽입 갱신. HTML 내부 상대링크(`../데모/`)는 함께 이동으로 자동 보존. → push 시 옛 GitHub Pages 경로는 404, 새 경로 `…/ExitGuard/pitch/…`로 살아남.

- 첫 직원 **헤르미온느(기획·명세, Opus)** 고용. MVP 기능명세서 작성 담당. 대본+껍데기+명부 완료.

## 기능 리스트업 (헤르미온느 위임 → 완료)
- 산출: `docs/spec/기능리스트.md`. 45개 기능(공통 CM 18 / 노무 LB 7 / 영업비밀 TS 6 / 보안 SEC 4).
- 진짜구현 33 / 시뮬 12, MVP 34 / Post-MVP 11. **Critical Path 18개**로 세로절단(김민수 퇴사 1건 → 3레일 → 리포트+증적 export) 관통 경로 도식화.
- 금지어 스캔 통과. PRODUCT §6 진짜/시뮬 경계 준수(AI 판례대조=진짜 / DLP·계정회수=시뮬).

### 대표님 결정 — [결정필요] 5건 확정
1. **CM-17 인증**: 로그인 O, 단 **관리자/일반사용자 2역할**로 단순화(4역할 아님). 일반사용자는 권한 부여받아 승인 수행. → MVP 승격.
2. **레일 액터**: **단일 담당자 통합**(중소 100인 이하 = 인사담당자 1명이 3레일 관리). 단 데이터모델은 **3팀 분리 확장성 고려**.
3. **CM-11 템플릿 편집**: **읽기전용 노출만 MVP 승격**(편집 UI는 Post-MVP). 해자 시연용.
4. **CM-13 전문가 연결**: **버튼+요약 stub**까지 MVP(실제 전달 채널 Post-MVP).
5. **TS-01 자산특정**(도비 처리안): **로직=진짜구현 / 입력=시뮬** 분리 표기(심사 방어).

## 팀 빌딩 (이번 세션)
- **헤르미온느 역할 확장**: 기획·명세 → **기획·설계(Product & Solution Spec)**. 기능명세에 더해 **API 명세 + 데이터 모델(계약)**까지 담당. 근거: 기능명세의 상태전이·데이터항목이 그대로 API/DB 계약으로 내려가 한 손이 잡아야 이음새가 안 깨짐. 경계 = "계약 문서까지"만, 구현 아키텍처(폴더구조·라이브러리·배포)는 도비+백엔드 몫. 산출물 `docs/spec/{api-spec,data-model}.md` 추가. 대본+껍데기+명부 갱신.
- **직원 3명 추가 고용**: 론(백엔드/Sonnet, FastAPI+AI대조엔진) · 해리(프론트/Sonnet, Next.js/shadcn) · 루나(디자이너/Opus, DESIGN.md·토큰). 각 대본+껍데기+명부 완료. 의존순서: 헤르미온느(계약)+루나(토큰) → 해리(FE)/론(BE) 병행. AI/RAG는 필요 시 론에서 분리(스네이프 등) 여지.

## 후속 세션 — 로고/아이콘 + 골격 아키텍처
- **아이콘 산출물**: `pitch/아이콘/`의 3개 컨셉 PNG(3rail/gate/shield) → JPG 변환(q92, 2048², resvg 아님 Pillow). 이후 대표님이 원한 건 **`exitguard-logo.svg` 그대로 PNG화** → `resvg-py`로 렌더(2400×2000, 투명배경, `exitguard-logo.png`). cairosvg는 Windows에 cairo 네이티브 없어 실패 → resvg-py로 우회. Python은 pip/PIL 없어 **`uv run --with <pkg>`** 로 일회성 실행(SVG 읽을 때 `encoding='utf-8'` 필수, cp949 디코드 에러).
- **README에 로고 적용**: 루트 `README.md` 최상단 `<img src="pitch/아이콘/exitguard-logo.png" width="240">`. (GitHub 한글경로 렌더 안 되면 URL 인코딩)
- **Python 3.13→3.12 하향**(대표님 지시): CLAUDE.md·PRODUCT.md·ron.md 3곳. (팀로그 참조)
- **FE/BE 골격 아키텍처 결정 완료**(팀로그 상세). 핵심: 계약 미확정이라 **"부팅&연결"까지만**, 단일repo, FastAPI 3단+SQLModel+create_all, Next.js App Router+shadcn+fetch, Redis 제외. DB는 **docker-compose Postgres 선택**했으나 로컬 Docker 미설치 → **Docker Desktop 설치 진행 중**(WSL2 유지, Windows containers 체크 X). 설치 완료되면 론·해리 병렬 위임.

## 다음 액션
- 헤르미온느가 결정 5건 반영해 `기능리스트.md` 갱신 중(백그라운드). 완료 시 도비 검증.
- 확정 후: **Critical Path 18개 우선으로 Feature Spec 상세 명세** 위임(레일별 분할 `기능명세서-공통/노무/영업비밀/보안.md` 권장) → 이어서 **API 명세·데이터 모델**(확장된 헤르미온느 역할).
- **루나에게 DESIGN.md 위임** — 기존 pitch 목업·로고·확정색 기반. 해리 FE 착수 전 토큰 확정 필요.
- (보류) BE/FE 코드 골격 — 계약·토큰 확정 후 론/해리.
- **⚠️ push 대기**: pitch/ 폴더 이동 커밋(74787e5)이 로컬에 있음 → dobby/·docs/spec/ push 시 함께 나감. 대표님이 pitch 외부 링크 확인 후 일괄 push.
- 심의지·라하나 대표님 성향 미파악 → 해당 세션에서 IDENTITY 개별 칸 채우기.
