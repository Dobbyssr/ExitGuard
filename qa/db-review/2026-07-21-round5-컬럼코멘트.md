# DB 감수 리포트 — round5 (컬럼 코멘트 소급 부착)

- **감수 일자:** 2026-07-21
- **감수 대상:**
  - 마이그레이션 `backend/alembic/versions/9ccfc39f8962_add_column_comments.py` (comment-only `alter_column` 94컬럼)
  - 모델 코멘트 SSOT: `app/db.py`(TimestampMixin) · `app/domains/{case,catalog,evidence,user,labor}/models.py`
  - 규칙: `backend/CLAUDE.md §9`("모든 컬럼 `comment=` 필수")
  - 계약: `docs/spec/data-model.md §3`(코어) · `docs/spec/노무/data-model.md §4`(LaborPrecedent)
- **종합 판정:** **OK** (🔴 0 · 🟡 0 · 🔵 1 · [결정필요] 1건은 신규 아님·패스스루)

---

## 감수 포인트별 결과

### 1. 코멘트-계약 일치 — 통과
10개 테이블 94개 컬럼 코멘트를 계약 필드 "설명"과 전수 대조. **창작·오기 0건.** 계약 표현을 간결한 한 줄로 정확히 축약했고, enum 값 나열(`exit_reason`·`status`·`kind`·`decision`·`origin` 등)이 계약 enum 정의와 100% 일치. 대표 대조 예:
- `cases.exit_reason` "…(voluntary/recommended_resignation/dismissal/contract_expiry)" = 계약 §2-2 4종 ✓
- `evidence.seq` "처리 순번(case 내 단조증가, 체인 순서 조회 키)" = §3-4-1 채번 규약 ✓
- `labor_precedents.case_no` "사건번호(제목에서 추출, 약 27%만 존재)" = 노무 §4-2 한계 실측(107/399) ✓
- `profiles.rail_map` "…({labor,trade_secret,security → rail_template_id})" = §3-7 ✓

### 2. 직역법 §17(판단/진단 함의) — 위반 0건
전 코멘트에 진단·위법·부당해고·패소·조언 어휘 **0건.** 민감 영역인 노무·증적 코멘트도 사실 서술로만 기재됨:
- `labor_precedents.matched_elements` "…판정 요구 요소 목록(대조 매칭 대상)" — "판정한다"가 아니라 "대조 매칭 대상" (안전 프레이밍)
- `evidence.integrity_hash`/`prev_hash` — "무결성"·"변경 탐지"의 기술 서술, 판단 함의 없음

### 3. 커버리지 — 완전
코멘트가 마이그레이션뿐 아니라 **모델(SSOT)에 실제로 박혀 있음**을 확인(`db.py` TimestampMixin `created_at`/`updated_at`, evidence·labor 모델 전 컬럼 `comment=`). 도비 확인 드리프트=0 → autogenerate가 이 리비전 후 빈 diff = **전 컬럼 부착 보장.** 설계상 컬럼 부재도 계약과 정합:
- `evidence`·`labor_precedents`·`standards`는 `created_at`/`updated_at` 미보유(각각 append-only·코퍼스 `ingested_at`·§3-5 `updated_at`만) → 계약대로. 누락 아님.

### 4. 마이그레이션 위생 — 통과
- **comment-only 확정**: 모든 `alter_column`이 `existing_type`·`existing_nullable`·`existing_server_default`·`autoincrement`를 보존한 채 `comment=`만 신규 부여. **타입/제약/인덱스/NULL 변경 0건.**
- `down_revision = "cdc2ffe29606"` ✓
- `downgrade()`가 전 컬럼 `comment=None`(+`existing_comment=` 명시)로 완결·가역 ✓
- 저위험 comment-only 마이그레이션. 정본 시드·데이터 무영향(코멘트는 메타데이터).

---

## 🔵 개선 (경미 · Post-MVP)

- 🔵[문서] `evidence`·`items`·`labor_precedents` 등의 시각 컬럼 코멘트 시제 표기가 혼재("생성 시각(UTC)" vs "처리 일시" vs "봉인 시각" vs "작성일자"). 의미 훼손은 없고 계약 원문 표현을 그대로 따른 결과라 **오기 아님.** 통일 필요도 낮음. — 권고: 손대지 말 것 [Post-MVP, 사실상 no-op].

## 의도적 비정규화 인정
해당 없음(코멘트 부착 라운드 — 스키마 구조 무변경).

## [결정필요] (신규 아님 — 패스스루)
- `labor_precedents.decided_on`이 모델에서 `date | None`(nullable)이나 계약 노무 §4는 `decided_on ● 필수`. **코멘트("작성일자")는 계약과 일치**하므로 round5 코멘트 감수 범위에서는 무결. 이 nullable 델타는 **론이 모델 주석(labor/models.py L102-105)에서 이미 `[결정필요]`로 자진 플래그**한 사항(실측 CSV에 388의 작성일자 근거 없어 창작 회피 목적)이며 이번 마이그레이션이 만든 문제가 아니다. 코어/노무 계약의 `decided_on` 필수도(●) 완화 여부만 헤르미온느와 확정하면 종결 — round5 커밋을 막지 않음.
